# aws_saa_quiz.github.io

AWS Solutions Architect - Associate (SAA-C03) practice quiz project.

## MCP

This repo is configured for AWS Documentation MCP via [`.mcp.json`](.mcp.json).

After pulling changes, restart your Codex session so MCP servers reload, then verify connectivity by listing MCP resources.
